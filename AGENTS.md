# 项目协作指南

## 这个仓库在做什么

**做一款肉鸽游戏**（`新游戏/`），参考三款已拆包的成品（`参考/`）。
参考资料是手段，新游戏是目的——两层分开放，不要把参考资料当成项目主体。

```
新游戏/          目标。设计阶段，尚未定题，还没有引擎与代码
  借鉴清单.md    12 条取舍的决策台账，全部待定
  设计/          定下来的机制才写文档
参考/            手段
  横向对比.md    三款的机制对比与不可比量纲清单
  研究约定.md    每个方向怎么查、条目类别、源码追溯路径
  结论/<游戏>/   12 篇人写的设计分析。日常读这层
  产物/<游戏>/   脚本抽出的原件：配置表、源码、资源、动画。要证据时下钻
  工具文档/      格式规范、解析取舍、已知坑、维护约定
源包/            APK 原件，已 gitignore
```

## 按任务去哪

| 任务 | 入口 |
|---|---|
| 新游戏的设计、选型、推进 | [新游戏/README.md](新游戏/README.md)、[新游戏/借鉴清单.md](新游戏/借鉴清单.md) |
| 查三款的玩法、数值、资产 | [参考/README.md](参考/README.md) 的路由表 |
| 三款之间的机制对比 | [参考/横向对比.md](参考/横向对比.md) |
| 怎么查、条目类别、追源码 | [参考/研究约定.md](参考/研究约定.md) |
| 改解析脚本 / 重跑流水线 / 提交 | [参考/工具文档/维护约定.md](参考/工具文档/维护约定.md) |

**版本以目录名为准**，不把这些版本的结论推广到其他版本。

## 查之前先知道这三条

1. **权重不是概率**。抽取权重要过池子、保底和重复规则，别直接当最终出现率报出去。
2. **Block 的数值只是客户端默认分支**。服务端可动态下发覆盖，包内又只是构建基线、线上随时被热更替换——引用它的任何数值都要带上这两条前提。
3. **资源存在 ≠ 进了池子**。三套资料都有登记了却没接进池子的内容，核对某条是否真在游戏里，要从消费端（数组、池、引用方）倒着查，不能只看资源目录或导出表的行数。

## 研究与回答原则

- 默认用中文交流，先回答用户的玩法或数值问题，再给出必要的依据。
- 结论层（`参考/结论/`）是研究入口；关键数值和机制应按任务需要核对产物层（`参考/产物/`）的数据表、原始资源及源码。发现结论与实现不一致时，明确指出差异。
- 跨游戏比较时分别注明游戏、版本和计算口径，比较机制与取舍，避免直接混用得分、DPS、经济或难度指标。已成文的对比结论与不可比量纲清单见 [参考/横向对比.md](参考/横向对比.md)。
- Brotato 的模拟 DPS、武器推荐和最优套装组合是特定假设下的推导，须说明属性投入、武器等级、槽位和是否允许重复等条件，不能直接视作实战排名。区分可玩内容与测试波次、空壳区域。
- Block 的包内权重表只是三层发牌决策链（端上 TFLite → 服务端推理 → 兜底权重）里的**兜底层**，不要当成线上实际跑的发牌逻辑；做算法归因用 `OFFER_TYPE_REAL_STRINGS` 那一列，上报名有 43 条与真实行为不一致。
- **Block 不是肉鸽**，目标是留存与 LTV。把它的做法当设计参考时说明目标差异与适用条件——按个体调难度、按情绪低点触发弹窗、逐关写死胜率这类机制搬进肉鸽会直接损伤「这一局是我打出来的」。三款的对照见 [参考/横向对比.md](参考/横向对比.md) §10。
- 区分游戏已有机制、分析推论和新的设计建议；提出可借鉴的方案时，说明适用条件与取舍。
- 给出结论时尽量附上对应文档、数据表或源码位置，便于继续追查。
- 研究和查询优先使用已有产物；仅在需要更新数据、修复解析或调整展示时运行或修改生成工具。

## 给新游戏做设计时

- 借鉴三款的任何机制，先看 [借鉴清单.md](新游戏/借鉴清单.md) 里这条是否已有裁定；已裁定按裁定走，未裁定先提出取舍再动手。
- 新游戏的设计文档写进 `新游戏/设计/`，**不要写进 `参考/`**。参考层只装对已有成品的研究。
- 引用参考结论时带上游戏与版本，并链到 `参考/结论/` 的对应位置。

## 改工具与资料

总则一条：**改生成器，不改产物**；抽取得到的输入除非任务明确要求，否则原样保留。

各游戏的流水线命令、目录含义与踩坑清单在 `参考/产物/<游戏>/README.md`，
共通的代码风格、编码口径、验证方式、提交与 PR 规范在
[参考/工具文档/维护约定.md](参考/工具文档/维护约定.md)。**动手改脚本或提交之前读一遍**；只做研究问答不需要。
