# 22 · 外观轴第二刀：窗口映射与 UI 引擎

Label: wayfinder:ticket · decision + task

## 为什么是现在

21 号票把复刻件放进了模拟器，第一次能与正版**同屏、同分辨率**比。CRT 修完之后，
剩下的偏差几乎全在两处：

1. **位置对不上。** 复刻件是固定 1512×806 的画布（只有房间 21×11.2 tile，没有留白）再 CSS 缩放；
   原作的窗口是房间 + 四周留白（22.6×12.4 tile），`love.resize` 按窗口等比算 `TILESCALE`、把房间居中
   （`main.lua:436`）。2560×1440 时每 tile 113.3 像素，房间原点在 (90.6, 85.7)——
   用这个算牌堆左上角是 (2180, 1043)，实机截图上量到的正是这里。
2. **UI 全是调试文字。** 左侧面板、盲注选择三张卡、按钮、回合收益……原作全由 `UIBox` 搭出来
   （`engine/ui.lua` 1085 行 + `UI_definitions.lua` 6607 行）。

## 裁定（自行裁定，依据如下，便于事后推翻）

**一、窗口映射照 `love.resize` 直译，落在相机上。**
世界坐标仍是固定的 72 像素 / tile（`coords.ts` 的 `toPx` 不动，10 号票的「T 全程 tile 单位」不受影响），
相机 `zoom = 实际每 tile 像素 / 72`、`scroll` 让房间原点落到 `love.resize` 算出的位置。
画布按**物理像素**开（`devicePixelRatio`），与原作「画布 = 屏幕分辨率」一致——
这顺带让 CRT 的 `scanlines` 回到原作的定义（`G.CANVAS:getPixelHeight()`）。
背景与 CRT 两个全屏 quad 跟着相机的可视区摆。

**二、UI 走「移植布局引擎 + 直译 UI 定义」，只有绘制与输入按 Phaser 惯例写。**
03 号票与地图写过「UI 是重写不是直译」。这条要细化：要像素级，最短的路是让**同一份定义算出同一份几何**，
所以 `UIBox:calculate_xywh` / `UIElement:set_wh` / `set_alignments` 这套布局（约 300 行、纯计算）原样移植，
`UI_definitions.lua` 里的定义函数逐个直译成返回同形状树的 TS 函数。
「重写」的只剩：`draw_self` / `draw_pixellated_rect` 换成 Phaser `Graphics`，文字换成 Phaser `Text`（m6x11plus），
点击 / 悬停走 Phaser 的输入。
- 布局部分**不 import Phaser**，放 `src/ui/`，能进单测
- 第一份直译的定义是 `create_UIBox_HUD`（左侧面板），验收拿模拟器截图量元素边框

**三、验收口径**：模拟器里两边都全屏 2560×1440，同一局面截图，量关键元素的像素边框。
允许 ±2 像素（字体光栅化与抗锯齿不可能逐像素一致）。

## 不在这一刀

- 卡牌的扇形排布与转角（`align_cards` 的 hand 分支，要 `VT.r`）——下一刀
- DynaText 的逐字弹跳、`juice_up`
- 悬停提示框（`h_popup`）

## 进展

**第一步：窗口映射**（已提交）。画布按物理像素开，相机照 `love.resize`。附带打通了复刻件的截图：
`?win=2560x1440` 钉死逻辑窗口，`tools/cdp.mjs snap` 经 DevTools 协议取回画布原图。

**第二步：UIBox 引擎 + 左侧面板。**
- `src/ui/uibox.ts`：`calculate_xywh` / `set_wh` / `set_alignments` / `align_to_major` 直译，连怪癖一起
- `src/ui/dynatext.ts`、`font.ts`（字宽表由 `tools/gen-font-metrics.mjs` 从 TTF 抽）、`colours.ts`、`format.ts`
  （`number_format`，`%.Nf` 复用 RNG 那边的精确展开——LuaJIT 对恰好一半取偶）
- `src/ui/definitions/hud.ts`：`create_UIBox_HUD` 逐节点直译；本地化由 `tools/gen-localization.mjs` 生成
- `src/game/ui-draw.ts`：像素圆角矩形（`draw_pixellated_rect` 的顶点表）、阴影、压花、T 节点文字、DynaText 逐字
- **布局的外部真值**：`tools/ui-oracle.py` 用 lupa（LuaJIT 2.1）原样加载原作的
  `object / node / moveable / ui / text / misc_functions / UI_definitions.lua`，只桩掉 LÖVE，
  跑同一份 HUD 定义导出 80 个元素的最终矩形。`uibox.test.ts` 逐个比到 1e-9，**第一次跑就全对**
- 盲注配色：`get_blind_main_colour` + `Blind:change_colour`。生成器原先把 `boss_colour` 剪掉了，现在保留

**实机对照**（2560×1440，选盲注界面）：面板各层颜色 ±3 级以内；元素位置与模型预测一致，
而实机整体比模型**低 3.5 像素**。原因不是布局：`update_canvas_juice`（`common_events.lua:1142`）让房间按
「屏幕震动」设置（缺省 50）做 ±0.02 tile 的正弦漂移、跟随光标的视差与 ±0.0015 弧度的旋转——原作的「呼吸」动画。
复刻件还没有它，下一步补；逐像素比对时两边把震动设成 0 来排除。

**第三步：房间摆动与按钮视差。**
- `src/game/room-juice.ts`：`update_canvas_juice` 直译。房间旋转以**屏幕上的** (ROOM.w/2, ROOM.h/2) 为轴
  （`Node:translate_container`），落到 Phaser 相机上要把原点设在那个轴点、滚动量补回房间偏移；
  背景与 CRT 两个全屏 quad 反转 −r 保持贴屏。`look.ts` 加 `screenshake`（缺省 50，`?shake=0` 关）
- 按钮的分层视差（`ui.lua:677`）：按钮及其子孙每带一层阴影就多挪 `0.4·sp/TILESIZE`，
  按钮里带阴影的文字挪两层（≈6.8 像素）。引擎里补上 `button_UIE` 的向下传递（`set_parent_child`）

**两边都关掉摆动之后**（实机 Settings → Game → Screenshake 0，复刻件 `?shake=0`）：
左侧面板 7 个区域的最佳对齐偏移全部 ≤1 像素（$ 那格除外，它有弹跳动画），整块平均逐像素差 26.6 → 3.8，
剩下的是字形边缘（原作 200px 栅格化后缩小 vs 复刻件按目标尺寸栅格化）。

> 实机的 Screenshake 比对时临时改成 0，2026-09-21 已调回 50（用户原来的值）。下次比对要关摆动时再改、用完调回。

**第四步：局内的区域与手牌。**
- CardArea 的底框与计数（`cardarea.lua:288` 的 `area_uibox`）直译，小丑区 / 消耗品区 / 手牌 / 牌堆四个框进了 Lua 真值
  （底板那行 `mid = true`，UIBox 以它居中、计数行挂在下面）。区域位置 `set_screen_positions` → `src/game/areas.ts`
- `align_cards` 的手牌 / 出牌区 / 小丑区分支 → `src/game/align-cards.ts`：扇形、两端下沉、±0.1 弧度转角、正弦微动。
  原作写进 `T`，但机制只用先后顺序、与 core 的 `i*CARD_W` 同序，所以 core 不动、表现层按原式摆
- 手牌区选牌时整体上移 1.9 tile、出牌时滑回（`cardarea.lua:236`），底框跟着走（`UIBox.followMajor`）
- 卡牌：按下标分深度（后压前）、阴影层（`dissolve` 的 shadow 模式、全部阴影在全部卡之下）、旋转、
  **`T.scale = 0.95`**（`card.lua:58`，所有卡都按 95% 画——漏了它卡牌大 5%、顶边高 8 像素）
- `dissolve` 的倾斜 uniform 在相机缩放后要用屏幕坐标（Phaser 喂给顶点着色器的已是变换后的位置）

**实机对照**（小盲注首手）：同一张牌的包围盒 299×210 对 299×210；手牌顶边逐列偏差平均 0，±5 像素是逐张正弦微动的相位差。

**第五步：出牌 / 排序 / 弃牌按钮与牌堆。**
- `create_UIBox_buttons` 直译（进了 Lua 真值）。引擎补了每帧跑的 `func`（`can_play` / `can_discard` 置灰、摘按钮名）、
  点击（`one_press`、点到按钮里的字转给按钮）、悬停色、按下回弹（阴影不再错开 + ×0.985）、描边与描边压花
- `G.F_MOBILE` 的 UI 分支（排序按钮移动版大一号）归进 `look.ts` 的 `mobileUi`，同受 `?look=mobile` 控制
- 牌堆：deck 分支的逐张错开、`thin_draw` 只画第 1 / 每第 9 / 最后一张；**牌堆里的牌背不走 shader**，
  第 4 张起乘灰（`card.lua:4557`）——堆叠边缘的灰线就是它，所以用能乘色的 `Image`
- 调试用的「出牌」「弃牌」按钮删了；其余调试按钮选牌时藏起来

**第六步：左上盲注面板（`create_UIBox_HUD_blind`）。**
- 定义与它的五个 `G.FUNCS`（debuff 行伸缩、The Wheel 前缀、目标分字号、奖励行）直译；小盲注与 The Head 两种形状进了 Lua 真值
- 引擎的一处怪癖：`calculate_xywh` 的 `_nt` 是**挂在节点上的持久表**。`HUD_blind_debuff` 在布局中途触发嵌套 `recalculate`，
  改写祖先节点的这张表，外层接着拿改过的值取 max——用局部变量会差 0.05 tile
- 盲注筹码：`BlindChips.png` 逐帧动画（10 fps、每行 21 帧）、±0.02 弧度摆动、阴影
- 两处移植坑：`blind_chip_UI_scale` 的 `if G.GAME.blind.chips`——**Lua 里 0 是真**，开局第一帧字号就是 0.7，
  照抄成 JS 的 `if (chips)` 就一直是 0.001，目标分不显示；T 节点的字号原先建视图时定死，现在跟 `config.scale` 走。
  `set_blind` 末尾的 `recalculate()` 也补上（同字数换数也要重排）
- 局内那两行调试文字删了，只留上一手的结算说明

**实机对照**（TESTSEED 小盲注首手）：面板整块平均逐像素差 5.9，看不出位置偏差，余下是筹码动画帧相位与字形边缘。

**第七步：选盲注界面（`create_UIBox_blind_select`）。**
- `create_UIBox_blind_choice` / `create_UIBox_blind_tag` / `UIBox_dyn_container` / 左侧的 `blind_prompt_box` 直译；
  每帧的 `blind_choice_handler` 直译（轮到的卡上提 0.9、描边；其余上提 0.2、盖半透明灰、按钮失效）。
  外层、三张卡、提示框共 5 个盒子进了 Lua 真值（TESTSEED 开局：小盲注轮到、The Head）
- 引擎补两处：**O 节点里装 UIBox**（卡片以 O 元素为 major、`bmi` 对齐）——原作 `initialize_VT` 对齐之后还会
  `recalculate()` 一次，这次不跑 `func`、用 `func` 改过的字号，The Wheel 前缀那种节点就在这里塌成 0 高，漏了差 0.13 tile；
  **`draw_after`**（先画孩子再画自己），没轮到的卡就是这样盖灰的
- 盲注筹码与标签精灵的阴影按 `sprite.lua:76`（错开 `shadow_height`、缩到 `1 − 0.2·shadow_height`）；`tags.png` 进素材
- DynaText 补分段着色（「Up the Ante」橙字）；core 的 `Run` 补 `blindState`（本 Ante 哪格打过 / 跳过）
- 调试按钮「下一关」「跳过盲注」在选盲注界面藏起来，卡片上的 Select / Skip Blind 接管

**实机对照**（TESTSEED 新开局的选盲注界面）：三张卡最佳对齐偏移都是 (0, 0)，平均逐像素差 2.7–3.7
（含背景流动纹理）；提示框 6.2（弹跳相位）。Skip → Economy Tag 把 $4 翻到 $8、Big 接替亮起 → Select 开打，全程在复刻件里走通。

**第八步：小丑区与消耗品区按 `align_cards` 摆；赢后不补牌（机制修正）。**
- 小丑 / 消耗品与手牌同一套摆放：`T.scale = 0.95`、阴影（卡面自己当 `shared_shadow`，高 0.1）、转角与正弦微动、按下标分深度。
  实机对照（TESTSEED 第二局买下 Raised Fist）：左边缘 1127 对 1125、顶边同为 88
- **机制偏差**：为了取带小丑的参照，两边重打了一局（两对 → 弃五张 → 葫芦），逐手的抽牌、分数、兑现都一致，
  但复刻件**赢下的那一手之后又补了 5 张**。原作 `update_hand_played`（`game.lua:3558`）够分或没手数就直接 `NEW_ROUND`，
  不进 `DRAW_TO_HAND`。多补的牌会在 The Wheel 这类 Boss 下多掷 RNG、回合结束时多算留手效果（Gold Card、Blue Seal……）。
  改后贪心 8 seed 3.125 → 2.875（ZZZZZZ 5 → 3）；挑牌 60 seed 3.8 → 3.767，各条策略对比的结论都不变（数字已更新）。
  这一局写进了 `emulator-truth.test.ts`
- 顺带记下：实机选牌时 HUD 的「Two Pair  lvl.1」中间有间距，复刻件挤在一起；商店与回合结算（Cash Out）仍是调试布局

**第九步：回合结算（Cash Out）。**
- `create_UIBox_round_evaluation` 与 `add_round_eval_row` 直译；引擎补 `add_child`（挂到某元素最后、整盒重排，绘制层按 `version` 重建）。
  `evaluate_round` 的事件队列摊平成时间表（`before` 先做后等、`after` 先等后做、`delay` 只等），场景按表逐行加、逐个 `$` 加，
  盲注行每加一个 `$` 左上面板的奖励少一个，`defeat` 时面板收起，最后出 Cash Out 按钮；**按了才入账、才进商店**
- 一处容易错的时序：每行的 `minw = G.round_eval.T.w − 0.51`，而容器内边距合计 0.52，每加一行盒子宽 0.01。
  `width` 是**调用** `add_round_eval_row` 时取的，而 `evaluate_round` 一口气调完所有行、事件后执行——
  所以实机每行都用空面板的宽度。Lua 真值一开始让事件立即执行，反而错了 0.01，改成排队后执行才对上
- 结算中左侧面板保持刚打完那一关的样子（分数 468、剩 2 手 3 弃、盲注配色直到 `defeat`），商店的暗红等 Cash Out 之后
- `v_dictionary` 进了本地化生成器（`Remaining Hands ($#1# each)` 这类）；`coin6` / `highlight1` 音效进素材

**实机对照**（TESTSEED 第二局的结算界面）：面板与左侧面板平均逐像素差 4.2 / 4.7，整体偏 (−1, −4) 是实机屏幕摆动（已调回 50）。

- HUD 小修：牌型名与等级之间的空格（`' '..localize('k_lvl')..level`）

**第十步：商店。**
- `G.UIDEF.shop`（外框、Next Round / Reroll 与 `can_reroll`、三个 CardArea 以 O 节点嵌入、竖排的「ANTE 1 VOUCHER」）、
  左上的 SHOP 招牌（`ShopSignAnimation.png` 4 帧）、`create_shop_card_ui` 的价签，进了 Lua 真值，第一次跑就全对。
  商品在区域里按 `align_cards` 的 shop 分支摆（补充包那一格 `card_w = 1.27·CARD_W`），价签挂在卡上、压在卡面之下
- **shader 的 alpha 口径**：原作 LÖVE 的 shader 按非预乘写，Phaser 上传纹理与混合都是预乘。
  叠加层（`booster` / `voucher` 等）在透明像素上加的颜色保持 alpha 0，预乘混合下变成整块发亮——补充包与优惠券一直是发白的矩形。
  所有卡面 shader 改为入口还原非预乘、出口再预乘（`texel_straight`）
- 调试布局（价格标签文字、「下一关 / 重掷」按钮、商店说明行）撤掉；没实现行为的商品仍在卡下标「⚠未实现」

**实机对照**（TESTSEED 第二局的第一个商店）：外框、按钮、小丑区、招牌最佳偏移都是 (3, 4)（实机屏幕摆动），
平均逐像素差按钮 5.1、小丑区 6.3、招牌 7.1。补充包的美术变体（`_1` / `_2`）两边不同——原作用的是无种子的全局随机。
买入、Reroll 置灰、Next Round 在复刻件里走通。

**第十一步：开包界面与背景换色。**
- 五个 `create_UIBox_*_pack` 只差标题与 `G.pack_cards` 的宽度，合成一个 `createBoosterPack`；奥秘 / 天体 / 小丑 / 巨型标准四种进了 Lua 真值，
  第一次（修掉标题串当成字符数组那处）就全对。外框挂手牌区（`tmi`、−2.2），包里的牌按 `align_cards` 的 consumeable 分支每帧摆（浮动 0.05）；
  Skip 走 `skip_booster` / `can_skip_booster`，调试的「跳过」按钮删了。开包时商店货架收起、只留招牌，关包时重建
- **背景一直是写死的小盲注绿**：`ease_background_colour_blind` 的背景那一半直译成 `backgroundFor`（Boss 用 `boss_colour`、终局 Boss 三色、五种包各一套），
  场景按 `ease_value` 的 lerp 缓动 0.6 秒。开包时左侧面板主色也跟着换（`packMainColour`）

**实机对照**（TESTSEED 第二局，买 Raised Fist 后开 Buffoon Pack，两张 Abstract Joker / Lucky Cat 与实机同）：
标题框、Skip、包里的牌、左侧面板最佳偏移一致（约 −7, −4，是两边屏幕摆动相位不同），对齐后平均逐像素差 3.5–8.7。
实机的 Lucky Cat 是剪影——档里还没发现过它（收藏进度），复刻件没有收藏这一层。

**第十二步：包里的消耗品当场用（机制修正，地图里记过的偏差）。**
- 原作开奥秘 / 幽灵包先 `draw_from_deck_to_hand` 从牌堆顶发一手牌，包里的塔罗 / 星球 / 幽灵牌**当场用**（`use_card`）；
  复刻件以前把它们拿进消耗品区，满了还挑不了
- 盲注之外的牌堆是逻辑状态：开局 `deck:shuffle()`（key `'shuffle'`）、**Cash Out 时 `shuffle('cashout'..ante)`**（`button_callbacks.lua:3028`，
  打完 Boss 时 Ante 已是新的）。`Run.idleDeck` 持有它，开包从顶上摸满手牌上限（按点数排），关包时手牌逐张插回最前（倒序压底），
  盲注之外新加的牌按 `emplace` 插最前。回合结束 `set_blind(nil)` 解除整副牌的 debuff
- `takeFromPack(i, highlighted)`：消耗品走 `can_use_consumeable`，用完才减次数、才关包（没挑走的还占着池子）；
  顺带修了 `can_select_card` 的**负片小丑满格也能拿**。`useConsumable` 拆出 `runConsumable` 与包共用
- 表现层：开包时的手牌走 `align_cards` 的 `*_PACK` 分支（`alignPackHand`：抬到手牌区上方 1.8 张牌高、±0.2 弧度），点选即目标；
  牌堆计数扣掉发出去的那手
- 两个 bot 跟着改：挑牌 bot 对发下来的手牌挑目标（`chooseTargets`）。**深度快照换数**：8 seed 两个均值都不变（逐 seed 有进有退）；
  60 seed 贪心 2.367 → 2.767（以前塔罗塞满格子又用不出来）、挑牌 3.767 → 3.95。存利息 / 跳盲 / 优惠券各条结论重核，方向都不变

**实机对照**（ALEEB 新开局：弃 TC TD 2D → 顺子 260 → 一对 A 324，商店 Trading Card / Rocket / Buffoon / Arcana）：
Arcana Pack 发的手牌 **QH TH TC 9D 8H 8D 7D 6C 逐张一致**、牌堆 44/52、包里 Temperance / The Empress / The Soul。钉进 `emulator-truth.test.ts`。
界面各区对齐偏移 ±7 像素内（手牌有 0.1 tile 的正弦浮动）。

- ~~没做完：The Soul 的宝石浮层、开包粒子~~ → 第十三步
- **实机上用包里塔罗的手势没试通**：单击只出提示，上划（快 / 慢 / 长短）都没触发 `Card:swipe_up`（它在原生层识别）。下次再试长按或拖到别处

**第十三步：浮层与粒子。**
- The Soul 的宝石（`G.shared_soul`，`centers` 图集 `{0,1}`）与传奇小丑 / Hologram 的 `soul_pos`（`floating_sprite`）：
  以卡心为轴额外缩放、额外转，先画一遍下移 `0.1 + 0.03·sin` 的阴影；两种摆动公式不同（`card.lua:4513`）。
  挂在 `PlacedLayers` 上每帧自己动。Hologram 原作用 `hologram` shader 画浮层，还没移植，先按 dissolve
- `engine/particles.lua` 直译成 `src/game/particles.ts`：奥秘 / 幽灵 / 标准包的闪光、天体包的星星与流星，参数照各 `update_*_pack`。
  颜色走无种子的 `math.random`，不碰 RNG 流。画在背景之上、所有 UI 与卡之下（原作经 `ROOM_ATTACH` 在 `I.MOVEABLE` 里画）。
  跟着包走：挑牌重建界面时不重开，关包淡出 1 秒后移除
- 实机对照（ALEEB 的 Arcana Pack）：宝石的形状、位置、阴影一致；粒子密度与配色一致（位置本来就随机）

**第十四步：卡牌 `VT` 缓动。**
- `engine/moveable.lua` 的 Major 一支直译成 `src/game/moveable.ts`（不 import Phaser，有单测）：`move_xy` 的弹簧与速度上限
  `70·move_dt`、`move_r` 的**横移前倾** `0.015·vel.x/dt`、`move_scale`（悬停 +0.05）、`juice_up` / `move_juice`。
  三个衰减系数按真实帧间隔算、推进用 `min(1/20, real_dt)`，照 `game.lua:2872`
- 扑克牌、小丑、消耗品的 `place` 只写 `T`，`postupdate` 里步进并按 `VT` 画（阴影视差仍按 `T` 算）。计分时的弹一下从补间换成
  `juice_up(0.6, 0.1)`（`card_eval_status_text`）
- 手牌重建时按 `Card` 接着上一个精灵的缓动走（塔罗改了牌面也不瞬移）；新摸的牌从牌堆飞进来，每张隔 0.1 秒
  （`draw_card` 的 `before, 0.1`）。近似：按手牌区顺序错开，原作是按摸牌顺序
- 页面里逐帧采样：一张牌约 10 帧从牌堆落到位，飞行中前倾约 −1.1 弧度，落定回到扇形转角；整条流程零报错

**第十五步：选中 → 按钮（买 / 卖 / 用 / 挑）。**
- `G.UIDEF.use_and_sell_buttons` 与 `create_shop_card_ui` 的 t2 / t3 直译成 `card-buttons.ts`，8 种挂法（小丑 SELL、消耗品 SELL + USE、
  包里 USE / SELECT、商店 BUY / REDEEM / OPEN / BUY & USE）进了 Lua 真值，第一次就全对
- 点卡 = 选中（抬起、`cardSlide1`），按钮以卡的可见位置为 major 每帧跟随；`func` 接到 `Run`（`can_sell_card` 结算中不能卖、
  `can_use_consumeable` 看选中的手牌、BUY & USE 用不了就整个藏起来）。以前是点卡即买、点小丑即卖、点包里的牌即拿
- `Run.buyAndUseConsumable`（`buy_from_shop` 的 `buy_and_use`：不查格子、不进区）；小丑可在盲注的选牌阶段卖
- 商店三个区改成每帧摆（选中要抬 `HIGHLIGHT_H`，价签跟着卡走）；优惠券与补充包精灵也换成 `PlacedLayers`（0.95、阴影、缓动）
- 复刻件全场只选一张（原作每个区各一张）。**移动版实机没有这些按钮**（单击出提示、上划买），这里取桌面的交互——浏览器是鼠标

**第十六步：卡牌提示框（`h_popup`）。**
- 本地化：`gen-descriptions.py` 用 lupa 原样加载 en-us.lua 导出全部 descriptions 与 misc 表，并原样跑 `Game:init_item_prototypes`
  导出 `P_CENTERS` / `P_TAGS`（提示框读原作的 center，不读复刻件按机制裁剪过的表）
- `loc_parse_string` / `loc_colour` / `localize`（descriptions / other / name 几支）直译成 `src/ui/localize.ts`；
  380 条描述逐行解析与原作一致（`loc-oracle.py`）
- `generate_UIBox_ability_table`（150 张小丑的 `loc_vars`）、`generate_card_ui`、`card_h_popup`、`create_badge` 直译成 `card-popup.ts`。
  `popup-oracle.py` 原样调用原作这几个函数：**273 张卡（小丑 / 塔罗 / 星球 / 幽灵 / 优惠券 / 补充包 / 强化）的提示文本逐行一致，
  8 种整框布局逐元素一致**，第一次就全对。Misprint 的随机滚动字先定格一帧
- 场景：悬停出框，朝向照 `align_h_popup`（商店 `cl`、上半屏 `bm`、其余 `tm`），附加说明（强化 / 版本 / 蜡封）照 `show_infotip` 挂在左边；
  选中的卡提示框常驻（触屏抬手就是 pointerout，原作移动版单击也常驻）。小丑区下面那行调试文字只留「⚠未实现」
- 修正：`Card:juice_up` 先把幅度 ×0.4、转角随机正负再交给 `Moveable.juice_up`，第十四步直接喂了原始参数，计分弹得过狠
- 实机对照（ALEEB 的 Arcana Pack，单击 The Empress）：主框与左侧「Mult Card +4 Mult」附加框的位置、样式一致；
  实机那张是「未发现」所以名字是 Not Discovered、没有角标——复刻件没有收藏进度，一律按已发现画

**第十七步：游戏结束与胜利界面。**
- `create_UIBox_game_over` / `create_UIBox_win` / `create_UIBox_round_scores_row` / `create_UIBox_generic_options` / `UIBox_button`
  直译成 `game-over.ts`，两张整框进了 Lua 真值（115 / 111 个元素），第一次（补上 `you_win_UI` 的 id）就全对
- core 补 `G.GAME.round_scores` 与 `hand_usage`（`round-scores.ts`）：出牌逐张、弃牌整批（The Hook 那次也算）、商店买牌 / 兑换 / 买包
  （标签送的包不算）、重掷、最佳一手取 floor。`new_collection` 在指定 seed 下恒 0。
  **近似**：最常用牌型并列时原作取 `pairs` 先遍历到的（LuaJIT 哈希序），复刻件取先打出的
- 场景：`overlay_menu` 照抄——挂房间 `cm`、从下方 10 tile 靠 VT 弹簧滑上来，背景色 alpha 0.3 秒缓到 0.8（胜利 0.5、无尽里输了是蓝），
  全屏挡板吞掉底下的点击与悬停，房间 jiggle +1（输了再 +3）；`negative` / `whoosh2` / `win` 音效与 `ui_assets.png` 进素材。
  **输了不调 `finishRound`**：原作 `end_round` 输了只跑小丑的 `end_of_round` 就进 GAME_OVER，底下留着那一关的盲注面板、分数与牌堆。
  以前要点「下一关」才判负、只出一行「失败」
- 按钮：Copy 写剪贴板；Endless 关窗接着打。复刻件没有主菜单与开局设置，**「New Run」换随机种子（`random_string(8)` 的字符分布）、
  「Main Menu」同种子**，都改 URL 的 `?seed` 整页重载
- 实机对照（TESTSEED 小盲注连出四手最小单张 4C 5D 5C 7H 输掉，两边统计一致：Best Hand 12、High Card (4)、Cards Played 4）：
  结束框、标题、统计、按钮、左上盲注面板最佳偏移都是 (−1, −8)（屏幕摆动相位），对齐后平均逐像素差 3.5–7.9。
  实机 2.5 秒后左边出 Jimbo 说俏皮话（`Card_Character` + 气泡）——**还没做**，位置空着
- 胜利界面实机够不着（要打穿 Ante 8），只核了 Lua 真值与复刻件里强制弹出的样子

**第十八步：Jimbo（`Card_Character`）。**
- `card_character.lua` 直译成 `src/game/jimbo.ts`：一张 `j_joker` 走 `start_materialize({BLUE, WHITE, RED})`（`dissolve` 0.6 秒 1→0、
  烧边两色、弹一下、同色碎屑半程停出）；自己的一团红蓝橙彩纸；`bm` 挂的对话气泡（`G.UIDEF.speech_bubble` → `speech-bubble.ts`，
  俏皮话走 `localize{type = 'quips'}`，移动版字号 ×1.45）；`say_stuff(5)` 每 0.13 秒一句 `voice<n>` 并弹一下
- 结束界面出现 2.5 秒后换进 `jimbo_spot`（输了 `lq_1..10`，无尽模式里输了没有；赢了 `wq_1..7`）
- 引擎补三处：shader quad 可以喂 `dissolve` 与烧边色（以前恒 0）；粒子可以 `attach` 到任意矩形、指定深度、半程改 `max`；
  `PlacedLayers` 可指定基准深度（Jimbo 要压在 overlay 之上）
- 素材：`voice1..11`、`whoosh1`、`crumple1..5`（184K）。本地化生成器导出 `QUIPS`，并对 `P_CENTERS` / `P_TAGS` 的键排序——
  lupa 每次 `pairs` 顺序不同，以前每重跑一次就整行变
- 实机对照（TESTSEED 结束界面）：卡与结束框同一偏移 (−3, −7)，对齐后平均逐像素差 6.6；`lq_6` 气泡算出来宽 386 px，实机量 385 px

**第十九步：Director's Cut 的「Reroll Boss $10」。**
- `G.blind_prompt_box` 里那个 `UIBox_button`（兑换过 Director's Cut / Retcon 才有）与每帧的 `reroll_boss_button`（钱够且本 Ante 没重掷过才亮，
  灰掉时两行字去阴影）直译，进了 Lua 真值（11 个元素）。`UIBox_button` 挪到 `ui-button.ts` 共用
- 调试按钮「重掷 Boss $10」删了。模拟器里真点：The Head → The Psychic、$20 → $10
- ~~Skipped 那一格不缺角标~~：**记错了**，实机上有斜着的「SKIPPED」戳，见第二十步

**第二十步：手上的标签（`G.HUD_tags`）与「SKIPPED」戳。**
- `add_tag`：标签在房间右下角叠成一列（第一个 `bri` 挂房间、x 外移 0.7，之后每个 `tm` 叠上一个），`run.tags` 变了整列重建。
  调试文字里的「标签：…」只留没实现行为的
- `create_UIBox_card_alert` 直译（进了 Lua 真值），`blind_choice_handler` 给跳过的那格盖「SKIPPED」（`tmi` 挂卡片、下移 2.2）。
  DynaText 的绘制补 `text_rot`（整串绕中心转）与 `bump_rate` / `bump_amount`
- **跳过盲注不再重建选盲注界面**：原作 `skip_blind` 只改 `blind_states`，跳过的卡保留原来的蓝 / 橙边框，由 handler 盖灰、收起标签行。
  复刻件以前整个重建，按 Skipped 取了黑色。拿到标签的调试提示也删了
- 实机对照（TESTSEED 连跳小盲、大盲，手上留 Investment Tag）：三张卡、右下角标签、左侧面板偏移一致（屏幕摆动），平均逐像素差 4.7–7.2
- Director's Cut 重掷 Boss 也改成只换 Boss 那张卡（`UIBox.replaceObject`：只重排新放进去的那个，外层不动——整盒重排会把 handler 挪走的标签行复位）。原作旧卡滑出、新卡滑入的动画还没做

**第二十一步：标签的提示框。**
- `Tag:get_uibox_table`（8 种标签按名字填 `loc_vars`，Orbital 没掷过牌型显示 `[poker hand]`）直译成 `tagAbilityTable`。
  `popup-oracle.py` 原样加载 `tag.lua` 调原文：**24 个标签 + Orbital 未掷两种，25 条逐行一致**，第一次就全对
- 右下角那列与选盲注卡上「or」旁边的标签，悬停出框（`cl`、左移 0.1），`paper1` / `tarot2` 两声。UIBoxView 只给按钮建命中区，
  标签的悬停区由场景另建、每帧跟着元素走（跳过的格子标签行被挪到 10 tile 以下，区也跟着走）。`Run.orbitalChoice(type)` 给提示框读
- 实机对照（TESTSEED 手上的 Investment Tag，单击）：框的样式、文字、朝向一致
- 没做：悬停时标签精灵的 `juice_up` 与 `hover_tilt`（实机上悬停的标签略大）

**第二十二步：Run Info。**
- `G.UIDEF.run_info` / `create_tabs` / `create_UIBox_current_hands` / `G.UIDEF.current_blinds`（`create_UIBox_blind_choice` 的 `run_info` 分支：
  顶上是状态牌 Current 红 / Skipped 蓝 / Defeated 灰 / Upcoming 橙，标签旁边是不能点的「Skip Reward」）/ `G.UIDEF.used_vouchers` 直译。
  `create_UIBox_generic_options` 抽成 `overlay.ts`（带 Back 键），`UIBox_button` 补 `choice` / `chosen`
- 三页都进了 Lua 真值：外框 28、牌型页 164、Blinds 页 100、Vouchers 页（四张）15 个元素，`change_tab` 用原文切页。
  两处引擎修正：**没有 `align` 的盒子**（`tab_contents` 里的页）`role.offset = offset`、照样跟着 major 走，以前停在 0；
  页盒子的 `func` 在第一次排版里就跑（Blinds 页的 `HUD_blind_debuff_prefix` 前缀字号变 0、整盒重排时塌掉，漏了差 0.13）
- 绘制：单选组被选中的按钮头上一个上下跳的红三角（`get_chosen_triangle_from_rect`）。overlay 挂载抽成 `mountOverlay`，结束界面与 Run Info 共用
- 场景：左侧面板的 Run Info 按钮打开它，标签页可切，Vouchers 页每格按 `align_cards` 的 voucher 分支摆卡（压在 overlay 之上），Back 关闭。
  「优惠券：…」调试文字撤了
- 实机对照（TESTSEED 连跳两格后的 Run Info）：Poker Hands、Blinds、Vouchers（空）三页与实机逐项一致
- 没做：牌型行悬停的说明与示例牌（`on_demand_tooltip` + `create_UIBox_hand_tip`）、优惠券卡的溶入动画与悬停提示框

还没做：各界面的滑入动画；选盲注界面的滑入动画、Boss 重掷时卡片滑出滑入、标签开包时卡片收起的动画；
Run Info 里的盲注页（`run_info` 分支）；DynaText 的弹入；标签、盲注筹码、牌堆的提示框；Hologram 的 hologram 着色器。
