# 仓库三层结构的约定更新

Type: wayfinder:task
Status: open

## Question

`复刻/` 落地后，把仓库的结构约定改对。

要改的：

- `README.md` 的目录树与「从哪读起」表——现在只写了两层
- `AGENTS.md` 的「这个仓库在做什么」目录树 + 「按任务去哪」表
- `新游戏/README.md` 如果提到两层结构，一并对齐
- 新写 `复刻/README.md`：这层装什么、为什么与 `参考/` `新游戏/` 分开、
  **只跑 localhost 不分发**这条前提写进去

要交代清楚三层的语义边界：`参考/` 是对已有成品的研究，`复刻/` 是对已有成品的重新实现，
`新游戏/` 是原创。以及一条新的约定：**复刻件不得对外部署**。

**阻塞已解除。** [素材与配置的接入方式](06-素材与配置的接入方式.md) 裁定：
素材拷进 `复刻/Balatro/public/assets/` 并入库（只拷用得上的，第一个切片 288K），
字体只带拉丁；配置从 Lua 直译，不碰 `配置CSV/`。

写 `复刻/README.md` 时要带上的两条：

- **`复刻/` 里有原始美术与音频**，所以「只跑 localhost、不部署不分发」
  这条前提要写在最显眼处，不能只写在 `Balatro/README.md` 里。
- **`复刻/` 不依赖 `参考/产物/配置CSV/`**。那层是给人看的研究产物；
  复刻件的配置源头是 `源码/` 里的 Lua。三层的语义边界因此更清楚了：
  `参考/结论/` 与 `参考/产物/配置CSV/` 是研究，`参考/产物/源码/` 与 `资源/` 是复刻的输入。
